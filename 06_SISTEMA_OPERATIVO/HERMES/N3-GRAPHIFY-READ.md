# N3-GRAPHIFY-READ

**Grantor:** Soberano 2026-08-23  
**Quién:** AutoClaw (Hermes runtime), Aether (este nodo Grok), no Z en este N3.  
**Graphify** es infraestructura (matriz: tríada Positrón · Graphify · Ollama). No es un agente. No es un JSON copiado al Desktop.

## STOP ahora

Si el dry-run o `inject_triad_graph.py` está a medias: **no apliques el patch**. No escribas nodos Hermes/Kairos/DIKE. `graphify/write` sigue DENY.

## Canónico (uno solo)

`https://raw.githubusercontent.com/wmejiasbcn-tech/Will-AI-Project-Lab/main/graph.json`

Hoy: `nodes=[]`, `links=[]`. Eso es DES-001 (otro N3, Z/grant). **No** se rellena desde el graph de 82 nodos del EliteBook ni desde un adjunto de chat.

## Este N3 (solo lectura)

1. Hermes: módulo **read-only** que GET el URL de arriba (o copia fechada con SHA-256). Si `nodes=[]`, el dashboard dice `grafo: vacío (DES-001)`. No inventa nodos.
2. AutoClaw: misma lectura antes de informar del ecosistema. No usa `waipl/agents/hermes/data/graph.json` como verdad.
3. Aether: misma URL. Ya la lee. No inyecta.
4. Kairos / DIKE: **no se instalan** aquí. Solo existen en la matriz. Meterlos en el grafo = N3-GRAPHIFY-WRITE, después de DES-001.

## Prohibido

- Patch de tríada sobre un graph local de 82 nodos
- Tratar esa copia como sistema nervioso
- `graphify/write` sin grant
- Declarar voz «confirmada por el Soberano» si no hay oído explícito
- Sustituir CANON-CIERRE por la lista A–E del N3-RUNTIME

## Éxito

Hermes `GET` canónico → log con `nodes=0` (hoy) + SHA. Cero nodos nuevos. Pie CANON-CIERRE de 7 casillas, las de `CANON-CIERRE.md`.
